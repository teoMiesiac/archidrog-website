export enum Color {
  WHITE,
  BLACK,
  PRIMARY,
  SECONDARY,
  ERROR,
  GREY,
  GREY_400,
  GREY_500
}

export enum Font {
  PRIMARY
}

export enum Border {
  REGULAR,
  MEDIUM_PRIMARY,
  MEDIUM_SECONDARY,
  MEDIUM_WHITE
}

export enum Shadow {
  PRIMARY,
  BUTTON,
  CAROUSEL,
  OFFER
}

export enum Size {
  HEADER,
  CONTENT_HORIZONTAL_PADDING,
  CONTENT_VERTICAL_PADDING
}

export enum ZIndex {
  CONTENT,
  HEADER,
  OVERLAY
}

export enum Transition {
  DEFAULT,
  LINK,
  BUTTON
}
