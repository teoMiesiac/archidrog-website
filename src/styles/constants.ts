export enum Color {
  WHITE,
  BLACK,
  PRIMARY,
  SECONDARY,
  ERROR
}

export enum Font {
  PRIMARY
}

export enum Border {
  REGULAR,
  MEDIUM_PRIMARY,
  MEDIUM_SECONDARY,
  MEDIUM_WHITE
}

export enum Shadow {
  PRIMARY,
  BUTTON,
  CAROUSEL
}

export enum Size {
  HEADER,
  CONTENT_HORIZONTAL_PADDING,
  CONTENT_VERTICAL_PADDING
}

export enum ZIndex {
  CONTENT,
  HEADER,
  OVERLAY
}

export enum Transition {
  DEFAULT,
  LINK,
  BUTTON
}
