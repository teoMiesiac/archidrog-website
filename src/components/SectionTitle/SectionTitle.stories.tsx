import React from 'react'
import SectionTitle from './SectionTitle.component'

const renderComponent = () => {
  return <SectionTitle>Example Title</SectionTitle>
}

export default {
  title: 'Shared/Section Title'
}

export const Default = () => renderComponent()
